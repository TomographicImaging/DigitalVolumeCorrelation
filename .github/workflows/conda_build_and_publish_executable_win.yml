name: build_executable_win

on:
  release:
    types: [published]
  push:
    branches: [ gui_devel ] # TODO: change to master
    tags:
      - '**'
  pull_request:
    branches: [ gui_devel ] # TODO: change to master

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest]
    steps:
    - uses: actions/checkout@v1
    - name: publish-to-conda
      uses: lauramurgatroyd/conda-package-publish-action@install_make
      with:
        subDir: 'conda-recipe/executable'
        channels: 'conda-forge'
        AnacondaToken: ${{ secrets.ANACONDA_TOKEN }}
        publish: ${{ github.event_name == 'push' && startsWith(github.event.ref, 'refs/tags') }}
        build_win: true
        convert_osx: false
        convert_win: false
