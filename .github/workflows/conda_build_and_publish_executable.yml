name: build_executable

on:
  release:
    types: [published]
  push:
    branches: [ gui_devel ] # TODO: change to master when merge app
    tags:
      - '**'
  pull_request:
    branches: [ gui_devel ] # TODO: change to master when merge app

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
    steps:
    - uses: actions/checkout@v1
    - name: publish-to-conda
      uses: paskino/conda-package-publish-action@fix_conversion_check
      with:
        subDir: 'conda-recipe/executable'
        channels: 'conda-forge'
        AnacondaToken: ${{ secrets.ANACONDA_TOKEN }}
        publish: ${{ github.event_name == 'push' && startsWith(github.event.ref, 'refs/tags') }}
        convert_win: false
